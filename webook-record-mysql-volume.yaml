# 描述mysql的存储结构需要的特征
apiVersion: v1
kind: PersistentVolume
metadata:
  name: webook-mysql-pvc
spec:
  storageClassName: record
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce

  hostPath:
    path: "/mnt/data"